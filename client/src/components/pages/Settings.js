import React from 'react';
import Setting from "../settings/Settings";

const Settings = () => {
    return (
        <div>
            <h1>Settings page</h1>
            <Setting />
        </div>
    );
};

export default Settings;
